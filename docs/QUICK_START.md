# HuanuCanvas 快速使用指南

## 🚀 **新用户快速开始**

### 1. 安装依赖
```bash
npm install
```

### 2. 启动开发环境
```bash
# 方式1: 启动完整开发环境 (推荐)
npm run electron:dev

# 方式2: 分别启动
npm run dev              # 前端: http://localhost:5173
npm run backend:dev      # 后端: http://localhost:8765
```

### 3. 构建项目
```bash
# 构建前端
npm run build

# 构建Electron应用
npm run electron:build
```

## 🔧 **常用命令**

| 命令 | 说明 |
|------|------|
| `npm install` | 安装所有依赖 |
| `npm run dev` | 启动前端开发服务器 |
| `npm run backend:dev` | 启动后端开发服务器 |
| `npm run electron:dev` | 启动完整开发环境 |
| `npm run build` | 构建前端应用 |
| `npm run electron:build` | 构建Electron应用 |
| `./deploy.sh install` | 一键部署安装 |
| `./manage.sh start dev` | 启动开发服务 |
| `./health.sh quick` | 快速健康检查 |

## 📁 **项目结构**
```
HuanuCanvas/
├── src/backend/      # 后端代码 (Express服务器)
├── src/frontend/    # 前端代码 (React应用)
├── electron/        # Electron配置
├── deployment/      # 部署配置
├── package.json     # 统一依赖配置
└── scripts/         # 管理脚本
```

## ✅ **整合优势**
- ✅ 单一依赖管理: `npm install` 一键安装所有依赖
- ✅ 简化构建流程: 现代化Monorepo结构
- ✅ 统一开发环境: 一条命令启动完整应用
- ✅ 维护成本降低: 配置文件从2个减少到1个

---

🎉 **依赖整合完成！现在享受简化的开发体验吧！**